// Archivo de compilación raíz (para abrir el proyecto en Android Studio).
// La app NO usa librerías externas: solo el SDK de Android y la librería estándar de Kotlin.
plugins {
    id("com.android.application") version "8.7.3" apply false
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
}
